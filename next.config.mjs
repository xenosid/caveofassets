/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'https://caveofassets.up.railway.app',
        ],
    },
}

export default nextConfig;
