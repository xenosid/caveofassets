/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {

        domains: [
			'localhost',
            'caveofassets.up.railway.app',
        ],        
    },
}

export default nextConfig;
